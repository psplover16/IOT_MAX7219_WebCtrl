import { defineStore } from "pinia";
export const usescreenWidth = defineStore("screenWidth", {
    state: () => ({
      min640: false
    }),
    actions: {
      changeMsg(e) {
        // this.msge;
      },
    },
});          
    